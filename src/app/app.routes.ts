/**
 * Root route table for composing public, authenticated user, and admin route trees.
 * Feature routes are added here lazily in their implementation phases.
 */
import { Routes } from '@angular/router';

export const routes: Routes = [];
