/* Production kaynaklarını kapsayan ve unit test dosyalarını dışlayan uygulama compiler girişidir. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": []
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.spec.ts"]
}
